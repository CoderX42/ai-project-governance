# 项目完整链路说明

> TODO：请按实际项目填充以下内容：
> 1. 替换 `{{PROJECT_NAME}}` 为项目名称
> 2. 替换第1节"项目目标"为项目真实目标
> 3. 替换第2节"核心运行参与者"为项目真实模块
> 4. 替换第3节"入口与装配关系"为项目真实入口和依赖关系
> 5. 替换第4节"状态与存储链路"为项目真实状态管理方案
> 6. 替换第5节及之后的各业务链路为项目真实业务流程

本文档面向 AI 与开发者，目标是让阅读者在最短时间内理解"项目做什么、怎么跑、数据怎么流、功能链路怎么串"，从而在新增功能时不漏逻辑、不误改边界。

使用建议：

1. 先阅读 [项目文件结构说明.md](./项目文件结构说明.md)
2. 再阅读本文
3. 最后阅读 [项目开发规范.md](./项目开发规范.md)

## 1. 项目目标

{{PROJECT_NAME}} 是一个用于 TODO：描述项目核心目标的系统。

它的核心价值是：TODO：用一两句话概括项目核心价值。

## 2. 核心运行参与者

### 2.1 前端 / 客户端

> TODO：列出前端主要文件和职责

例如：

- `frontend/src/App.tsx` + `frontend/src/pages/*`
  职责：
  - 展示配置与状态
  - 接收用户输入
  - 向后台发送命令
  - 接收后台广播并更新 UI
  - 动态渲染步骤/页面列表

### 2.2 后台 / 服务端

> TODO：列出后台主要文件和职责

例如：

- `backend/main.py` 或 `server/index.js`
  职责：
  - 服务端入口
  - 装配所有模块
  - 统一承接请求/消息
  - 协调业务执行
  - 管理状态、自动运行、通信

### 2.3 辅助模块 / 工具层

> TODO：列出辅助模块的分布和职责

例如：

- 分布在 `utils/`、`providers/`、`shared/` 下。
- 职责：
  - 抽离第三方 provider 的纯逻辑
  - 抽离数据匹配与转换
  - 抽离共享流程、自动运行流程和运行时基础设施

## 3. 入口与装配关系

### 3.1 项目入口

> TODO：描述项目启动入口和主要配置文件

例如：

- `package.json` 声明启动脚本
- `manifest.json` 或 `pyproject.toml` 声明依赖和入口
- 前端入口：`src/main.tsx`
- 后台入口：`src/server.ts`

### 3.2 模块装配

> TODO：描述模块如何被加载和装配

例如：

- 入口文件通过 `import` / `require` 依次加载共享数据、纯工具、provider 逻辑、后台桥接层、共享流程层、运行时与消息路由层、业务执行模块。
- 因此入口文件现在更像：常量定义中心、模块依赖装配器、极少量保留函数、事件挂接入口。

### 3.3 流程/步骤注册

> TODO：描述流程定义和执行器如何映射

例如：

- `shared/flow-definitions.ts` 提供共享流程元数据，前后台共同使用。
- `backend/executors/registry.ts` 负责把"流程元数据"映射到"执行器"。
- 这意味着：流程顺序靠 `order`，执行文件名靠语义，新增步骤时不需要重命名后续文件。

### 3.4 日志与步骤号链路

> TODO：描述日志和步骤标识的传递方式

例如：

- 后台统一通过结构化日志条目写入步骤标识。
- 前端只读取结构化元数据渲染步骤标签，禁止再用正则从日志正文解析步骤号。
- 不同模式下复用执行器时，必须先解析当前可见步骤号，再传入日志和完成信号。

## 4. 状态与存储链路

### 4.1 运行态存储

> TODO：描述运行时状态保存在哪里、包含什么

例如：

- 保存在内存 / SessionStorage / Redis / 临时缓存中。
- 包含：当前步骤状态、当前 UI 模式、会话字段、当前用户标识、当前配置值、自动运行轮次信息、标签/连接注册表等。

### 4.2 持久配置存储

> TODO：描述持久化配置保存在哪里、包含什么

例如：

- 保存在 `localStorage` / 配置文件 / 数据库中。
- 包含：用户偏好设置、账号配置、API 地址、开关状态、运行历史等。

注意：

- 运行态模式不属于持久配置，也不参与导入/导出。
- 运行态模式不能混进持久配置。

### 4.3 状态广播

> TODO：描述前后台/各模块之间如何同步状态

例如：

- 后台通过消息/事件向前端广播：日志、状态变更、数据更新、自动运行状态等。
- 前端根据广播更新 UI，不在 UI 层重新推断后台结果。

## 5. 业务链路

> TODO：按项目实际业务，分节描述核心流程

### 5.1 主流程示例

> TODO：描述项目最核心的业务流程

建议格式：

1. 步骤A：负责什么，入口文件是哪些
2. 步骤B：负责什么，入口文件是哪些
3. ...

每个步骤说明：
- 输入是什么
- 输出是什么
- 失败时怎么处理
- 和前后步骤的衔接关系

### 5.2 分支流程/特殊模式

> TODO：描述项目中的特殊模式或分支流程

例如：

- Plus 模式 / 高级模式
- 贡献模式
- 手机号注册 vs 邮箱注册
- 各 provider 的差异化链路

### 5.3 Provider / 适配器链路

> TODO：描述项目中各第三方 provider 的接入方式

建议格式：

#### Provider A

- 组成：哪些文件
- 链路：1. ... 2. ... 3. ...
- 补充约定：特殊行为和维护注意事项

#### Provider B

...

## 6. 通信链路

### 6.1 就绪/握手机制

> TODO：描述模块间的就绪确认机制

例如：

- 客户端加载后发送就绪信号。
- 服务端收到后注册当前连接、标记就绪、冲刷排队命令。

### 6.2 队列与重试

> TODO：描述请求队列、超时和重试机制

例如：

- 页面切换导致暂时失联时，服务端不会立刻误判彻底失败。
- 用户停止后，底层轮询和重试等待会尽快中断。

## 7. 维护约定

> TODO：列出各核心链路的关键维护约定

例如：

- 某模块的职责边界
- 某配置项的生效范围
- 某链路的错误处理方式
- 修改某模块时必须同步检查的其他模块