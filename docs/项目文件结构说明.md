# 项目文件结构说明

> TODO：请按实际项目结构填充以下内容。
> 规则：
> 1. 新增、删除、重命名非忽略文件后，必须同步更新本文件。
> 2. 如果文件职责发生明显变化，也必须同步更新。
> 3. 本文档不保留"增量补充""结构补充"一类追加章节；结构变化时，直接更新对应文件条目与职责说明。

本文档列出当前仓库中所有"非忽略文件"，并说明每个文件的作用。

不纳入本清单的忽略目录：

- `.github/`
- `.git/`
- `.vscode/`
- `docs/md/`（如存在，作为本地开发方案目录）
- `node_modules/`、`venv/`、`__pycache__/` 等依赖/构建目录
- 构建产物目录（如 `dist/`、`build/`）

更新规则：

- 新增、删除、重命名非忽略文件后，必须同步更新本文件。
- 如果文件职责发生明显变化，也必须同步更新本文件中的说明。
- 本文档不保留"增量补充""结构补充"一类追加章节；结构变化时，直接更新对应文件条目与职责说明。

## 根目录

> TODO：列出根目录下所有非忽略文件及其职责

例如：

- `.gitignore`：定义仓库忽略规则。
- `LICENSE`：项目许可证文件。
- `README.md`：面向使用者的项目介绍、安装说明、能力清单与操作指引。
- `CLAUDE.md`：AI 协作总规则。
- `{{PACKAGE_FILE}}`：依赖与脚本定义文件。
- `开发者AI开发与PR流程.md`：AI 开发与 PR 提交流程说明。
- `项目文件结构说明.md`：当前文件，维护整个仓库非忽略文件的结构与职责索引。
- `项目完整链路说明.md`：面向 AI/开发者的完整功能链路说明。
- `项目开发规范.md`：面向 AI/开发者的项目开发规范、约束与变更检查清单。

## `src/` 或主要源码目录

> TODO：列出主要源码目录结构和文件职责

例如：

### `src/components/`

- `src/components/Button.tsx`：通用按钮组件。
- `src/components/Modal.tsx`：通用弹窗组件。

### `src/utils/`

- `src/utils/api.ts`：API 请求封装。
- `src/utils/format.ts`：数据格式化工具。

### `src/pages/` 或 `src/views/`

- `src/pages/Home.tsx`：首页。
- `src/pages/Settings.tsx`：设置页。

## `backend/` 或 `server/`（如适用）

> TODO：列出后端/服务端目录结构

例如：

- `backend/main.py`：服务端入口。
- `backend/routes/`：路由定义。
- `backend/models/`：数据模型。
- `backend/services/`：业务服务层。

## `tests/` 或 `__tests__/`（如适用）

> TODO：列出测试文件和覆盖范围

例如：

- `tests/unit/`：单元测试。
- `tests/integration/`：集成测试。
- `tests/e2e/`：端到端测试。

## `docs/`（如适用）

> TODO：列出文档目录结构

例如：

- `docs/使用教程.md`：面向用户的操作指引。
- `docs/架构设计.md`：面向开发者的架构说明。

## `scripts/`（如适用）

> TODO：列出脚本目录

例如：

- `scripts/setup.sh`：开发环境初始化脚本。
- `scripts/deploy.sh`：部署脚本。

## `public/` 或 `assets/`（如适用）

> TODO：列出静态资源目录

例如：

- `public/favicon.ico`
- `public/logo.png`
- `assets/styles/`